pip install pyvirtualdisplay
pip install sqlalchemy
pip install selenium

# wsgi app
pip install flup

# firfox comes with webdriver
apt-get install iceweasel

# X-server frame buffer
apt-get install xvfb
